pipeline {
    agent any
    
    parameters {
        string(
            name: 'run-mode',
            defaultValue: 'test',
            description: 'Select the test run mode',
        )
    }
    
    stages {
        stage('SampleStage') {
            steps{
                echo "This is declarative pipeline sample"
                echo "${env.JOB_NAME}"
            }
        }
        
        stage('Checkout') {
            steps{
                git url : "https://github.com/sureshdubey/JenkinsCourse.git", branch : "master"
            }
        }
        
        stage('Build') {
            steps{
                sh 'mvn install'
            }
        }
    }
}
